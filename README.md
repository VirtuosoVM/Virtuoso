# Virtuoso

> A virtual machine host for Discord powered by VMware.

<p align="center"><img width=200 alt="virtuoso logo" src="./assets/logo.png" /></p>

---

todo: make this readme better

1. install vmware (player is free)
2. setup your vm in there, boot it up, install the vmware tools, set up a guest account, and enable auto login to it (probably the one you're using as the creds, unless you're providing admin creds), then shutdown the vm
3. set up the config using the template as config.json
