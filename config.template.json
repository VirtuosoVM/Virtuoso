{
    "discord": {
        "token": "your bot's token",
        "prefix": "v!",
        "owner_id": "your discord ID",
        "authorised_user_ids": {
            "a user's id": [
                "the groups they're in",
                "every user is in default automatically"
            ],
            "example": [
                "admin"
            ]
        },
        "rcon": {
            "enable": false,
            "rcon_user_ids": [
                "user ids that can use rcon commands",
                "this is a dangerous permission to grant! they can use it to escalate permissions on both the host and guest os"
            ]
        }
    },
    "vmware": {
        "vmrun_path": "path to the vmrun executable",
        "host_type": "ws",
        "default_options": {
            "vm_password": "password to decrypt the vm or blank",
            "disconnect_sound": true,
            "credentials": {
                "default": {
                    "username": "default group guest os username",
                    "password": "default group guest os password"
                },
                "admin": {
                    "username": "admin group guest os username",
                    "password": "super secure admin group guest os password!"
                },
                "another group": {
                    "username": "this group's guest os username",
                    "password": "this group's guest os password"
                }
            }
        },
        "vm_list": {
            "example": {
                "name": "Name of the VM",
                "description": "Description of the VM",
                "vmx": "path to the vmx file",
                "options_override": {
                    "credentials": {
                        "admin": {
                            "username": "admin group guest os username override",
                            "password": "super secure admin group guest os password override!"
                        },
                        "new user group": {
                            "username": "you cannot define a new group here!",
                            "password": "only groups defined in default_options can be applied to users, so this will be a useless credential"
                        }
                    }
                }
            }
        },
        "screenshot_dir": "./screenshots/"
    },
    "commands": {
        "disabled": [
            "command to disable"
        ]
    }
}