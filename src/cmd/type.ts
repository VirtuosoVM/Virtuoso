// Types characters into the VM's keyboard.

import { CommandCall } from "../types";

import * as embeds from "../embed_generator";

const call: CommandCall = (in_message, data) => {
    const { args, client, VMRun } = data;
}
